# Generated by OmopViewer 0.3.0
# Be careful editing this file

ui <- bslib::page_navbar(
  title = shiny::tags$span(
    shiny::tags$img(
      src = "ohdsi_logo.svg",
      width = "auto",
      height = "46px",
      class = "me-3",
      alt = "logo"
    ),
    "DrugUtilisation Package"
  ),
  theme = bslib::bs_theme(),
  bslib::nav_panel(
    title = "Background",
    icon = shiny::icon("disease"),
    backgroundCard("background.md")
  ),
  bslib::nav_panel(
    title = "Database details",
    icon = shiny::icon("list"),
    bslib::nav_panel(
      title = "Snapshot",
      icon = shiny::icon("clipboard-list"),
      bslib::layout_sidebar(
        sidebar = bslib::sidebar(
          shinyWidgets::pickerInput(
            inputId = "summarise_omop_snapshot_cdm_name",
            label = "CDM name",
            choices = choices$summarise_omop_snapshot_cdm_name,
            selected = selected$summarise_omop_snapshot_cdm_name,
            multiple = TRUE,
            options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
          ),
          shinyWidgets::pickerInput(
            inputId = "summarise_omop_snapshot_variable_name",
            label = "Variable name",
            choices = choices$summarise_omop_snapshot_variable_name,
            selected = selected$summarise_omop_snapshot_variable_name,
            multiple = TRUE,
            options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
          ),
          position = "left"
        ),
        bslib::navset_card_tab(
          bslib::nav_panel(
            title = "Table Snapshot",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shinyWidgets::pickerInput(
                    inputId = "summarise_omop_snapshot_table_format",
                    label = "Format",
                    choices = c("docx", "png", "pdf", "html"),
                    selected = "docx",
                    multiple = FALSE,
                    options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                  ),
                  shiny::downloadButton(outputId = "summarise_omop_snapshot_table_download", label = "Download table")
                ),
                class = "text-end"
              ),
              gt::gt_output("summarise_omop_snapshot_table") |>
                shinycssloaders::withSpinner()
            )
          )
        )
      )
    )
  ),
  bslib::nav_menu(
    title = "Results",
    icon = icon("list"),
    bslib::nav_panel(
      title = "CohortCharacteristics",
      icon = shiny::icon("users-gear"),
      navset_card_tab(
        bslib::nav_panel(
          title = "Cohort Count",
          icon = shiny::icon("users"),
          bslib::layout_sidebar(
            sidebar = bslib::sidebar(
              shinyWidgets::pickerInput(
                inputId = "summarise_cohort_count_cdm_name",
                label = "CDM name",
                choices = choices$summarise_cohort_count_cdm_name,
                selected = selected$summarise_cohort_count_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_cohort_count_cohort_name",
                label = "Cohort name",
                choices = choices$summarise_cohort_count_cohort_name,
                selected = selected$summarise_cohort_count_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_cohort_count_variable_name",
                label = "Variable name",
                choices = choices$summarise_cohort_count_variable_name,
                selected = selected$summarise_cohort_count_variable_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_cohort_count_table_name",
                label = "Table name",
                choices = choices$summarise_cohort_count_table_name,
                selected = selected$summarise_cohort_count_table_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              position = "left"
            ),
            bslib::navset_card_tab(
              bslib::nav_panel(
                title = "Tidy",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::downloadButton(outputId = "summarise_cohort_count_tidy_download", label = "Download csv")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_cohort_count_tidy_columns",
                        label = "Columns",
                        choices = c("cdm_name", "cohort_name", "table_name", "variable_name", "variable_level"),
                        selected = c("cdm_name", "cohort_name", "variable_name", "variable_level"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::checkboxInput(
                        inputId = "summarise_cohort_count_tidy_pivot_estimates",
                        label = "Pivot estimates",
                        value = TRUE
                      ),
                      position = "right"
                    ),
                    DT::DTOutput("summarise_cohort_count_tidy") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Table Counts",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_cohort_count_table_format",
                        label = "Format",
                        choices = c("docx", "png", "pdf", "html"),
                        selected = "docx",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::downloadButton(outputId = "summarise_cohort_count_table_download", label = "Download table")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      sortable::bucket_list(
                        header = NULL,
                        sortable::add_rank_list(
                          text = "None",
                          labels = c("variable_name", "estimate_name"),
                          input_id = "summarise_cohort_count_table_none"
                        ),
                        sortable::add_rank_list(
                          text = "Header",
                          labels = "cohort_name",
                          input_id = "summarise_cohort_count_table_header"
                        ),
                        sortable::add_rank_list(
                          text = "Group columns",
                          labels = "cdm_name",
                          input_id = "summarise_cohort_count_table_group_column"
                        ),
                        sortable::add_rank_list(
                          text = "Hide",
                          labels = c("variable_level", "table_name"),
                          input_id = "summarise_cohort_count_table_hide"
                        )
                      ),
                      position = "right"
                    ),
                    gt::gt_output("summarise_cohort_count_table") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Plot Counts",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::numericInput(
                        inputId = "summarise_cohort_count_plot_width",
                        label = "Width",
                        value = 15
                      ),
                      shiny::numericInput(
                        inputId = "summarise_cohort_count_plot_height",
                        label = "Height",
                        value = 15
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_cohort_count_plot_units",
                        label = "Units",
                        choices = c("px", "cm", "inch"),
                        selected = "cm",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::numericInput(
                        inputId = "summarise_cohort_count_plot_dpi",
                        label = "DPI",
                        value = 300
                      ),
                      shiny::downloadButton(outputId = "summarise_cohort_count_plot_download", label = "Download plot")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_cohort_count_plot_facet",
                        label = "Facet",
                        choices = c("cdm_name", "cohort_name", "table_name"),
                        selected = "cdm_name",
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_cohort_count_plot_colour",
                        label = "Colour",
                        choices = c("cdm_name", "cohort_name", "table_name"),
                        selected = "cohort_name",
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      position = "right"
                    ),
                    shiny::plotOutput("summarise_cohort_count_plot") |>
                      shinycssloaders::withSpinner()
                  )
                )
              )
            )
          )
        ),
        bslib::nav_panel(
          title = "Cohort Attrition",
          icon = shiny::icon("layer-group"),
          bslib::layout_sidebar(
            sidebar = bslib::sidebar(
              shinyWidgets::pickerInput(
                inputId = "summarise_cohort_attrition_cdm_name",
                label = "CDM name",
                choices = choices$summarise_cohort_attrition_cdm_name,
                selected = selected$summarise_cohort_attrition_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_cohort_attrition_cohort_name",
                label = "Cohort name",
                choices = choices$summarise_cohort_attrition_cohort_name,
                selected = choices$summarise_cohort_attrition_cohort_name[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_cohort_attrition_variable_name",
                label = "Variable name",
                choices = choices$summarise_cohort_attrition_variable_name,
                selected = selected$summarise_cohort_attrition_variable_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              position = "left"
            ),
            bslib::navset_card_tab(
              bslib::nav_panel(
                title = "Tidy",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::downloadButton(outputId = "summarise_cohort_attrition_tidy_download", label = "Download csv")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_cohort_attrition_tidy_columns",
                        label = "Columns",
                        choices = c("cdm_name", "cohort_name", "reason", "reason_id", "age_group", "cohort_definition_id", "days_prior_observation", "end_date", "gap_era", "limit", "prior_drug_observation", "prior_use_washout", "requirements_at_entry", "sex", "start_date", "table_name", "target_cohort_name", "time_at_risk", "variable_name", "variable_level"),
                        selected = c("cdm_name", "cohort_name", "reason", "reason_id", "variable_name", "variable_level"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::checkboxInput(
                        inputId = "summarise_cohort_attrition_tidy_pivot_estimates",
                        label = "Pivot estimates",
                        value = TRUE
                      ),
                      position = "right"
                    ),
                    DT::DTOutput("summarise_cohort_attrition_tidy") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Table Attrition",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_cohort_attrition_table_format",
                        label = "Format",
                        choices = c("docx", "png", "pdf", "html"),
                        selected = "docx",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::downloadButton(outputId = "summarise_cohort_attrition_table_download", label = "Download table")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      sortable::bucket_list(
                        header = NULL,
                        sortable::add_rank_list(
                          text = "None",
                          labels = "reason",
                          input_id = "summarise_cohort_attrition_table_none"
                        ),
                        sortable::add_rank_list(
                          text = "Header",
                          labels = "variable_name",
                          input_id = "summarise_cohort_attrition_table_header"
                        ),
                        sortable::add_rank_list(
                          text = "Group columns",
                          labels = c("cdm_name", "cohort_name"),
                          input_id = "summarise_cohort_attrition_table_group_column"
                        ),
                        sortable::add_rank_list(
                          text = "Hide",
                          labels = c("variable_level", "reason_id", "estimate_name", "age_group", "cohort_definition_id", "days_prior_observation", "end_date", "gap_era", "limit", "prior_drug_observation", "prior_use_washout", "requirements_at_entry", "sex", "start_date", "table_name", "target_cohort_name", "time_at_risk"),
                          input_id = "summarise_cohort_attrition_table_hide"
                        )
                      ),
                      position = "right"
                    ),
                    gt::gt_output("summarise_cohort_attrition_table") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Diagram",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::numericInput(
                        inputId = "summarise_cohort_attrition_diagram_width",
                        label = "Width (px)",
                        value = 2000
                      ),
                      shiny::downloadButton(outputId = "summarise_cohort_attrition_diagram_download", label = "Download Diagram")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_cohort_attrition_diagram_show",
                        label = "Show",
                        choices = c("subjects", "records"),
                        selected = c("subjects", "records"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      position = "right"
                    ),
                    DiagrammeR::grVizOutput("summarise_cohort_attrition_diagram") |>
                      shinycssloaders::withSpinner()
                  )
                )
              )
            )
          )
        ),
        bslib::nav_panel(
          title = "Cohort Characteristics",
          icon = shiny::icon("users-gear"),
          bslib::layout_sidebar(
            sidebar = bslib::sidebar(
              shinyWidgets::pickerInput(
                inputId = "summarise_characteristics_cdm_name",
                label = "CDM name",
                choices = choices$summarise_characteristics_cdm_name,
                selected = selected$summarise_characteristics_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_characteristics_cohort_name",
                label = "Cohort name",
                choices = choices$summarise_characteristics_cohort_name,
                selected = selected$summarise_characteristics_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_characteristics_age_group",
                label = "Age group",
                choices = choices$summarise_characteristics_age_group,
                selected = "overall",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_characteristics_sex",
                label = "Sex",
                choices = choices$summarise_characteristics_sex,
                selected = "overall",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_characteristics_variable_name",
                label = "Variable name",
                choices = choices$summarise_characteristics_variable_name,
                selected = "Number subjects",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_characteristics_estimate_name",
                label = "Estimate name",
                choices = choices$summarise_characteristics_estimate_name,
                selected = selected$summarise_characteristics_estimate_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              position = "left"
            ),
            bslib::navset_card_tab(
              bslib::nav_panel(
                title = "Tidy",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::downloadButton(outputId = "summarise_characteristics_tidy_download", label = "Download csv")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_characteristics_tidy_columns",
                        label = "Columns",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex", "table", "window", "value", "table_name", "variable_name", "variable_level"),
                        selected = c("cdm_name", "cohort_name", "age_group", "sex", "table", "window", "value", "variable_name", "variable_level"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::checkboxInput(
                        inputId = "summarise_characteristics_tidy_pivot_estimates",
                        label = "Pivot estimates",
                        value = TRUE
                      ),
                      position = "right"
                    ),
                    DT::DTOutput("summarise_characteristics_tidy") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Table Characteristics",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_characteristics_table_format",
                        label = "Format",
                        choices = c("docx", "png", "pdf", "html"),
                        selected = "docx",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::downloadButton(outputId = "summarise_characteristics_table_download", label = "Download table")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      sortable::bucket_list(
                        header = NULL,
                        sortable::add_rank_list(
                          text = "None",
                          labels = c("age_group", "sex", "variable_name", "variable_level", "estimate_name"),
                          input_id = "summarise_characteristics_table_none"
                        ),
                        sortable::add_rank_list(
                          text = "Header",
                          labels = c("cdm_name", "cohort_name"),
                          input_id = "summarise_characteristics_table_header"
                        ),
                        sortable::add_rank_list(
                          text = "Group columns",
                          labels = character(),
                          input_id = "summarise_characteristics_table_group_column"
                        ),
                        sortable::add_rank_list(
                          text = "Hide",
                          labels = c("table", "window", "value", "table_name"),
                          input_id = "summarise_characteristics_table_hide"
                        )
                      ),
                      position = "right"
                    ),
                    gt::gt_output("summarise_characteristics_table") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Plot Characteristics",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::numericInput(
                        inputId = "summarise_characteristics_plot_width",
                        label = "Width",
                        value = 15
                      ),
                      shiny::numericInput(
                        inputId = "summarise_characteristics_plot_height",
                        label = "Height",
                        value = 15
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_characteristics_plot_units",
                        label = "Units",
                        choices = c("px", "cm", "inch"),
                        selected = "cm",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::numericInput(
                        inputId = "summarise_characteristics_plot_dpi",
                        label = "DPI",
                        value = 300
                      ),
                      shiny::downloadButton(outputId = "summarise_characteristics_plot_download", label = "Download plot")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_characteristics_plot_plot_type",
                        label = "Plot type",
                        choices = c("boxplot", "barplot", "scatterplot"),
                        selected = "barplot",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_characteristics_plot_facet",
                        label = "Facet",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex"),
                        selected = "cdm_name",
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_characteristics_plot_colour",
                        label = "Colour",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex"),
                        selected = "cohort_name",
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      position = "right"
                    ),
                    shiny::plotOutput("summarise_characteristics_plot") |>
                      shinycssloaders::withSpinner()
                  )
                )
              )
            )
          )
        ))
    ),
    bslib::nav_panel(
      title = "IncidencePrevalence",
      icon  = icon("chart-bar"),
      bslib::navset_card_tab(
        bslib::nav_panel(
          title = "Incidence",
          icon = shiny::icon("chart-line"),
          bslib::layout_sidebar(
            sidebar = bslib::sidebar(
              shinyWidgets::pickerInput(
                inputId = "incidence_cdm_name",
                label = "CDM name",
                choices = choices$incidence_cdm_name,
                selected = selected$incidence_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_outcome_cohort_name",
                label = "Outcome cohort name",
                choices = choices$incidence_outcome_cohort_name,
                selected = selected$incidence_outcome_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_incidence_start_date",
                label = "Incidence start date",
                choices = choices$incidence_incidence_start_date,
                selected = selected$incidence_incidence_start_date,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_analysis_interval",
                label = "Analysis interval",
                choices = choices$incidence_analysis_interval,
                selected = selected$incidence_analysis_interval,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_analysis_censor_cohort_name",
                label = "Analysis censor cohort name",
                choices = choices$incidence_analysis_censor_cohort_name,
                selected = selected$incidence_analysis_censor_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_analysis_complete_database_intervals",
                label = "Analysis complete database intervals",
                choices = choices$incidence_analysis_complete_database_intervals,
                selected = selected$incidence_analysis_complete_database_intervals,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_analysis_outcome_washout",
                label = "Analysis outcome washout",
                choices = choices$incidence_analysis_outcome_washout,
                selected = selected$incidence_analysis_outcome_washout,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_analysis_repeated_events",
                label = "Analysis repeated events",
                choices = choices$incidence_analysis_repeated_events,
                selected = selected$incidence_analysis_repeated_events,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_denominator_age_group",
                label = "Denominator age group",
                choices = choices$incidence_denominator_age_group,
                selected = "0 to 150",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_denominator_days_prior_observation",
                label = "Denominator days prior observation",
                choices = choices$incidence_denominator_days_prior_observation,
                selected = selected$incidence_denominator_days_prior_observation,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_denominator_end_date",
                label = "Denominator end date",
                choices = choices$incidence_denominator_end_date,
                selected = selected$incidence_denominator_end_date,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_denominator_requirements_at_entry",
                label = "Denominator requirements at entry",
                choices = choices$incidence_denominator_requirements_at_entry,
                selected = selected$incidence_denominator_requirements_at_entry,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_denominator_sex",
                label = "Denominator sex",
                choices = choices$incidence_denominator_sex,
                selected = "Both",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_denominator_start_date",
                label = "Denominator start date",
                choices = choices$incidence_denominator_start_date,
                selected = selected$incidence_denominator_start_date,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_denominator_target_cohort_name",
                label = "Denominator target cohort name",
                choices = choices$incidence_denominator_target_cohort_name,
                selected = selected$incidence_denominator_target_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_denominator_time_at_risk",
                label = "Denominator time at risk",
                choices = choices$incidence_denominator_time_at_risk,
                selected = selected$incidence_denominator_time_at_risk,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_variable_name",
                label = "Variable name",
                choices = choices$incidence_variable_name,
                selected = selected$incidence_variable_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_estimate_name",
                label = "Estimate name",
                choices = choices$incidence_estimate_name,
                selected = selected$incidence_estimate_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              position = "left"
            ),
            bslib::navset_card_tab(
              bslib::nav_panel(
                title = "Tidy",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::downloadButton(outputId = "incidence_tidy_download", label = "Download csv")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "incidence_tidy_columns",
                        label = "Columns",
                        choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "incidence_start_date", "incidence_end_date", "analysis_interval", "analysis_censor_cohort_name", "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk", "variable_name", "variable_level"),
                        selected = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "incidence_start_date", "incidence_end_date", "analysis_interval", "variable_name", "variable_level"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::checkboxInput(
                        inputId = "incidence_tidy_pivot_estimates",
                        label = "Pivot estimates",
                        value = TRUE
                      ),
                      position = "right"
                    ),
                    DT::DTOutput("incidence_tidy") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Table Incidence",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shinyWidgets::pickerInput(
                        inputId = "incidence_table_format",
                        label = "Format",
                        choices = c("docx", "png", "pdf", "html"),
                        selected = "docx",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::downloadButton(outputId = "incidence_table_download", label = "Download table")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      sortable::bucket_list(
                        header = NULL,
                        sortable::add_rank_list(
                          text = "None",
                          labels = c("incidence_start_date", "incidence_end_date", "denominator_age_group", "denominator_sex"),
                          input_id = "incidence_table_none"
                        ),
                        sortable::add_rank_list(
                          text = "Header",
                          labels = "estimate_name",
                          input_id = "incidence_table_header"
                        ),
                        sortable::add_rank_list(
                          text = "Group columns",
                          labels = c("cdm_name", "outcome_cohort_name"),
                          input_id = "incidence_table_group_column"
                        ),
                        sortable::add_rank_list(
                          text = "Hide",
                          labels = c("denominator_cohort_name", "analysis_interval", "analysis_censor_cohort_name", "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk"),
                          input_id = "incidence_table_hide"
                        )
                      ),
                      position = "right"
                    ),
                    gt::gt_output("incidence_table") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Plot Incidence",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::numericInput(
                        inputId = "incidence_plot_width",
                        label = "Width",
                        value = 15
                      ),
                      shiny::numericInput(
                        inputId = "incidence_plot_height",
                        label = "Height",
                        value = 15
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "incidence_plot_units",
                        label = "Units",
                        choices = c("px", "cm", "inch"),
                        selected = "cm",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::numericInput(
                        inputId = "incidence_plot_dpi",
                        label = "DPI",
                        value = 300
                      ),
                      shiny::downloadButton(outputId = "incidence_plot_download", label = "Download plot")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "incidence_plot_x",
                        label = "x axis",
                        choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "incidence_start_date", "incidence_end_date", "analysis_interval", "analysis_censor_cohort_name", "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk"),
                        selected = "incidence_start_date",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "incidence_plot_facet",
                        label = "Facet",
                        choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "incidence_start_date", "incidence_end_date", "analysis_interval", "analysis_censor_cohort_name", "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk"),
                        selected = "cdm_name",
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "incidence_plot_colour",
                        label = "Colour",
                        choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "incidence_start_date", "incidence_end_date", "analysis_interval", "analysis_censor_cohort_name", "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk"),
                        selected = "outcome_cohort_name",
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      position = "right"
                    ),
                    shiny::plotOutput("incidence_plot") |>
                      shinycssloaders::withSpinner()
                  )
                )
              )
            )
          )
        ),
        bslib::nav_panel(
          title = "Incidence Attrition",
          icon = shiny::icon("layer-group"),
          bslib::layout_sidebar(
            sidebar = bslib::sidebar(
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_cdm_name",
                label = "CDM name",
                choices = choices$incidence_attrition_cdm_name,
                selected = selected$incidence_attrition_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_analysis_censor_cohort_name",
                label = "Analysis censor cohort name",
                choices = choices$incidence_attrition_analysis_censor_cohort_name,
                selected = selected$incidence_attrition_analysis_censor_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_analysis_complete_database_intervals",
                label = "Analysis complete database intervals",
                choices = choices$incidence_attrition_analysis_complete_database_intervals,
                selected = selected$incidence_attrition_analysis_complete_database_intervals,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_analysis_outcome_washout",
                label = "Analysis outcome washout",
                choices = choices$incidence_attrition_analysis_outcome_washout,
                selected = selected$incidence_attrition_analysis_outcome_washout,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_analysis_repeated_events",
                label = "Analysis repeated events",
                choices = choices$incidence_attrition_analysis_repeated_events,
                selected = selected$incidence_attrition_analysis_repeated_events,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_denominator_age_group",
                label = "Denominator age group",
                choices = choices$incidence_attrition_denominator_age_group,
                selected = "0 to 150",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_denominator_days_prior_observation",
                label = "Denominator days prior observation",
                choices = choices$incidence_attrition_denominator_days_prior_observation,
                selected = selected$incidence_attrition_denominator_days_prior_observation,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_denominator_end_date",
                label = "Denominator end date",
                choices = choices$incidence_attrition_denominator_end_date,
                selected = selected$incidence_attrition_denominator_end_date,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_denominator_requirements_at_entry",
                label = "Denominator requirements at entry",
                choices = choices$incidence_attrition_denominator_requirements_at_entry,
                selected = selected$incidence_attrition_denominator_requirements_at_entry,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_denominator_sex",
                label = "Denominator sex",
                choices = choices$incidence_attrition_denominator_sex,
                selected = "Both",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_denominator_start_date",
                label = "Denominator start date",
                choices = choices$incidence_attrition_denominator_start_date,
                selected = selected$incidence_attrition_denominator_start_date,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_denominator_target_cohort_name",
                label = "Denominator target cohort name",
                choices = choices$incidence_attrition_denominator_target_cohort_name,
                selected = selected$incidence_attrition_denominator_target_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_denominator_time_at_risk",
                label = "Denominator time at risk",
                choices = choices$incidence_attrition_denominator_time_at_risk,
                selected = selected$incidence_attrition_denominator_time_at_risk,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_variable_name",
                label = "Variable name",
                choices = choices$incidence_attrition_variable_name,
                selected = selected$incidence_attrition_variable_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              position = "left"
            ),
            bslib::navset_card_tab(
              bslib::nav_panel(
                title = "Tidy",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::downloadButton(outputId = "incidence_attrition_tidy_download", label = "Download csv")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "incidence_attrition_tidy_columns",
                        label = "Columns",
                        choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "reason", "reason_id", "analysis_censor_cohort_name", "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk", "variable_name", "variable_level"),
                        selected = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "reason", "reason_id", "variable_name", "variable_level"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::checkboxInput(
                        inputId = "incidence_attrition_tidy_pivot_estimates",
                        label = "Pivot estimates",
                        value = TRUE
                      ),
                      position = "right"
                    ),
                    DT::DTOutput("incidence_attrition_tidy") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Table Incidence Attrition",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shinyWidgets::pickerInput(
                        inputId = "incidence_attrition_table_format",
                        label = "Format",
                        choices = c("docx", "png", "pdf", "html"),
                        selected = "docx",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::downloadButton(outputId = "incidence_attrition_table_download", label = "Download table")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      sortable::bucket_list(
                        header = NULL,
                        sortable::add_rank_list(
                          text = "None",
                          labels = "reason",
                          input_id = "incidence_attrition_table_none"
                        ),
                        sortable::add_rank_list(
                          text = "Header",
                          labels = "variable_name",
                          input_id = "incidence_attrition_table_header"
                        ),
                        sortable::add_rank_list(
                          text = "Group columns",
                          labels = c("cdm_name", "outcome_cohort_name"),
                          input_id = "incidence_attrition_table_group_column"
                        ),
                        sortable::add_rank_list(
                          text = "Hide",
                          labels = c("denominator_cohort_name", "estimate_name", "reason_id", "variable_level", "analysis_censor_cohort_name", "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", "denominator_age_group", "denominator_sex", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk"),
                          input_id = "incidence_attrition_table_hide"
                        )
                      ),
                      position = "right"
                    ),
                    gt::gt_output("incidence_attrition_table") |>
                      shinycssloaders::withSpinner()
                  )
                )
              )
            )
          )
        ),
        bslib::nav_panel(
          title = "Prevalence",
          icon = shiny::icon("chart-column"),
          bslib::layout_sidebar(
            sidebar = bslib::sidebar(
              shinyWidgets::pickerInput(
                inputId = "prevalence_cdm_name",
                label = "CDM name",
                choices = choices$prevalence_cdm_name,
                selected = selected$prevalence_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_outcome_cohort_name",
                label = "Outcome cohort name",
                choices = choices$prevalence_outcome_cohort_name,
                selected = selected$prevalence_outcome_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_prevalence_start_date",
                label = "Prevalence start date",
                choices = choices$prevalence_prevalence_start_date,
                selected = selected$prevalence_prevalence_start_date,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_prevalence_end_date",
                label = "Prevalence end date",
                choices = choices$prevalence_prevalence_end_date,
                selected = selected$prevalence_prevalence_end_date,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_analysis_interval",
                label = "Analysis interval",
                choices = choices$prevalence_analysis_interval,
                selected = selected$prevalence_analysis_interval,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_analysis_complete_database_intervals",
                label = "Analysis complete database intervals",
                choices = choices$prevalence_analysis_complete_database_intervals,
                selected = selected$prevalence_analysis_complete_database_intervals,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_analysis_full_contribution",
                label = "Analysis full contribution",
                choices = choices$prevalence_analysis_full_contribution,
                selected = selected$prevalence_analysis_full_contribution,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_analysis_type",
                label = "Analysis type",
                choices = choices$prevalence_analysis_type,
                selected = selected$prevalence_analysis_type,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_denominator_age_group",
                label = "Denominator age group",
                choices = choices$prevalence_denominator_age_group,
                selected = "0 to 150",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_denominator_days_prior_observation",
                label = "Denominator days prior observation",
                choices = choices$prevalence_denominator_days_prior_observation,
                selected = selected$prevalence_denominator_days_prior_observation,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_denominator_end_date",
                label = "Denominator end date",
                choices = choices$prevalence_denominator_end_date,
                selected = selected$prevalence_denominator_end_date,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_denominator_requirements_at_entry",
                label = "Denominator requirements at entry",
                choices = choices$prevalence_denominator_requirements_at_entry,
                selected = selected$prevalence_denominator_requirements_at_entry,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_denominator_sex",
                label = "Denominator sex",
                choices = choices$prevalence_denominator_sex,
                selected = "Both",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_denominator_start_date",
                label = "Denominator start date",
                choices = choices$prevalence_denominator_start_date,
                selected = selected$prevalence_denominator_start_date,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_denominator_target_cohort_name",
                label = "Denominator target cohort name",
                choices = choices$prevalence_denominator_target_cohort_name,
                selected = selected$prevalence_denominator_target_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_denominator_time_at_risk",
                label = "Denominator time at risk",
                choices = choices$prevalence_denominator_time_at_risk,
                selected = selected$prevalence_denominator_time_at_risk,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_variable_name",
                label = "Variable name",
                choices = choices$prevalence_variable_name,
                selected = selected$prevalence_variable_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_estimate_name",
                label = "Estimate name",
                choices = choices$prevalence_estimate_name,
                selected = selected$prevalence_estimate_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              position = "left"
            ),
            bslib::navset_card_tab(
              bslib::nav_panel(
                title = "Tidy",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::downloadButton(outputId = "prevalence_tidy_download", label = "Download csv")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "prevalence_tidy_columns",
                        label = "Columns",
                        choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "prevalence_start_date", "prevalence_end_date", "analysis_interval", "analysis_complete_database_intervals", "analysis_full_contribution", "analysis_type", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk", "variable_name", "variable_level"),
                        selected = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "prevalence_start_date", "prevalence_end_date", "analysis_interval", "variable_name", "variable_level"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::checkboxInput(
                        inputId = "prevalence_tidy_pivot_estimates",
                        label = "Pivot estimates",
                        value = TRUE
                      ),
                      position = "right"
                    ),
                    DT::DTOutput("prevalence_tidy") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Table Prevalence",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shinyWidgets::pickerInput(
                        inputId = "prevalence_table_format",
                        label = "Format",
                        choices = c("docx", "png", "pdf", "html"),
                        selected = "docx",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::downloadButton(outputId = "prevalence_table_download", label = "Download table")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      sortable::bucket_list(
                        header = NULL,
                        sortable::add_rank_list(
                          text = "None",
                          labels = c("prevalence_start_date", "prevalence_end_date", "denominator_age_group", "denominator_sex"),
                          input_id = "prevalence_table_none"
                        ),
                        sortable::add_rank_list(
                          text = "Header",
                          labels = "estimate_name",
                          input_id = "prevalence_table_header"
                        ),
                        sortable::add_rank_list(
                          text = "Group columns",
                          labels = c("cdm_name", "outcome_cohort_name"),
                          input_id = "prevalence_table_group_column"
                        ),
                        sortable::add_rank_list(
                          text = "Hide",
                          labels = c("denominator_cohort_name", "analysis_interval", "analysis_complete_database_intervals", "analysis_full_contribution", "analysis_type", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk"),
                          input_id = "prevalence_table_hide"
                        )
                      ),
                      position = "right"
                    ),
                    gt::gt_output("prevalence_table") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Plot Prevalence",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::numericInput(
                        inputId = "prevalence_plot_width",
                        label = "Width",
                        value = 15
                      ),
                      shiny::numericInput(
                        inputId = "prevalence_plot_height",
                        label = "Height",
                        value = 15
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "prevalence_plot_units",
                        label = "Units",
                        choices = c("px", "cm", "inch"),
                        selected = "cm",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::numericInput(
                        inputId = "prevalence_plot_dpi",
                        label = "DPI",
                        value = 300
                      ),
                      shiny::downloadButton(outputId = "prevalence_plot_download", label = "Download plot")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "prevalence_plot_x",
                        label = "x axis",
                        choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "prevalence_start_date", "prevalence_end_date", "analysis_interval", "analysis_complete_database_intervals", "analysis_full_contribution", "analysis_type", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk"),
                        selected = "prevalence_start_date",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "prevalence_plot_facet",
                        label = "Facet",
                        choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "prevalence_start_date", "prevalence_end_date", "analysis_interval", "analysis_complete_database_intervals", "analysis_full_contribution", "analysis_type", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk"),
                        selected = "cdm_name",
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "prevalence_plot_colour",
                        label = "Colour",
                        choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "prevalence_start_date", "prevalence_end_date", "analysis_interval", "analysis_complete_database_intervals", "analysis_full_contribution", "analysis_type", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk"),
                        selected = "outcome_cohort_name",
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      position = "right"
                    ),
                    shiny::plotOutput("prevalence_plot") |>
                      shinycssloaders::withSpinner()
                  )
                )
              )
            )
          )
        ),
        bslib::nav_panel(
          title = "Prevalence Attrition",
          icon = shiny::icon("layer-group"),
          bslib::layout_sidebar(
            sidebar = bslib::sidebar(
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_cdm_name",
                label = "CDM name",
                choices = choices$prevalence_attrition_cdm_name,
                selected = selected$prevalence_attrition_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_analysis_complete_database_intervals",
                label = "Analysis complete database intervals",
                choices = choices$prevalence_attrition_analysis_complete_database_intervals,
                selected = selected$prevalence_attrition_analysis_complete_database_intervals,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_analysis_full_contribution",
                label = "Analysis full contribution",
                choices = choices$prevalence_attrition_analysis_full_contribution,
                selected = selected$prevalence_attrition_analysis_full_contribution,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_analysis_type",
                label = "Analysis type",
                choices = choices$prevalence_attrition_analysis_type,
                selected = selected$prevalence_attrition_analysis_type,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_denominator_age_group",
                label = "Denominator age group",
                choices = choices$prevalence_attrition_denominator_age_group,
                selected = "0 to 150",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_denominator_days_prior_observation",
                label = "Denominator days prior observation",
                choices = choices$prevalence_attrition_denominator_days_prior_observation,
                selected = selected$prevalence_attrition_denominator_days_prior_observation,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_denominator_end_date",
                label = "Denominator end date",
                choices = choices$prevalence_attrition_denominator_end_date,
                selected = selected$prevalence_attrition_denominator_end_date,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_denominator_requirements_at_entry",
                label = "Denominator requirements at entry",
                choices = choices$prevalence_attrition_denominator_requirements_at_entry,
                selected = selected$prevalence_attrition_denominator_requirements_at_entry,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_denominator_sex",
                label = "Denominator sex",
                choices = choices$prevalence_attrition_denominator_sex,
                selected = "Both",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_denominator_start_date",
                label = "Denominator start date",
                choices = choices$prevalence_attrition_denominator_start_date,
                selected = selected$prevalence_attrition_denominator_start_date,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_denominator_target_cohort_name",
                label = "Denominator target cohort name",
                choices = choices$prevalence_attrition_denominator_target_cohort_name,
                selected = selected$prevalence_attrition_denominator_target_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_denominator_time_at_risk",
                label = "Denominator time at risk",
                choices = choices$prevalence_attrition_denominator_time_at_risk,
                selected = selected$prevalence_attrition_denominator_time_at_risk,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_variable_name",
                label = "Variable name",
                choices = choices$prevalence_attrition_variable_name,
                selected = selected$prevalence_attrition_variable_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              position = "left"
            ),
            bslib::navset_card_tab(
              bslib::nav_panel(
                title = "Tidy",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::downloadButton(outputId = "prevalence_attrition_tidy_download", label = "Download csv")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "prevalence_attrition_tidy_columns",
                        label = "Columns",
                        choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "reason", "reason_id", "analysis_complete_database_intervals", "analysis_full_contribution", "analysis_type", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk", "variable_name", "variable_level"),
                        selected = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "reason", "reason_id", "variable_name", "variable_level"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::checkboxInput(
                        inputId = "prevalence_attrition_tidy_pivot_estimates",
                        label = "Pivot estimates",
                        value = TRUE
                      ),
                      position = "right"
                    ),
                    DT::DTOutput("prevalence_attrition_tidy") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Table Prevalence Attrition",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shinyWidgets::pickerInput(
                        inputId = "prevalence_attrition_table_format",
                        label = "Format",
                        choices = c("docx", "png", "pdf", "html"),
                        selected = "docx",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::downloadButton(outputId = "prevalence_attrition_table_download", label = "Download table")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      sortable::bucket_list(
                        header = NULL,
                        sortable::add_rank_list(
                          text = "None",
                          labels = "reason",
                          input_id = "prevalence_attrition_table_none"
                        ),
                        sortable::add_rank_list(
                          text = "Header",
                          labels = "variable_name",
                          input_id = "prevalence_attrition_table_header"
                        ),
                        sortable::add_rank_list(
                          text = "Group columns",
                          labels = c("cdm_name", "outcome_cohort_name"),
                          input_id = "prevalence_attrition_table_group_column"
                        ),
                        sortable::add_rank_list(
                          text = "Hide",
                          labels = c("denominator_cohort_name", "estimate_name", "reason_id", "variable_level", "analysis_interval", "analysis_complete_database_intervals", "analysis_full_contribution", "analysis_type", "denominator_age_group", "denominator_sex", "denominator_days_prior_observation", "denominator_end_date", "denominator_requirements_at_entry", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk"),
                          input_id = "prevalence_attrition_table_hide"
                        )
                      ),
                      position = "right"
                    ),
                    gt::gt_output("prevalence_attrition_table") |>
                      shinycssloaders::withSpinner()
                  )
                )
              )
            )
          )
        ))
    ),
    bslib::nav_panel(
      title = "DrugUtilisation",
      icon = shiny::icon("pills"),
      bslib::navset_card_tab(
        bslib::nav_panel(
          title = "Proportion of patients covered",
          icon = shiny::icon("chart-gantt"),
          bslib::layout_sidebar(
            sidebar = bslib::sidebar(
              shinyWidgets::pickerInput(
                inputId = "summarise_proportion_of_patients_covered_cdm_name",
                label = "CDM name",
                choices = choices$summarise_proportion_of_patients_covered_cdm_name,
                selected = selected$summarise_proportion_of_patients_covered_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_proportion_of_patients_covered_age_group",
                label = "Age group",
                choices = choices$summarise_proportion_of_patients_covered_age_group,
                selected = "overall",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_proportion_of_patients_covered_sex",
                label = "Sex",
                choices = choices$summarise_proportion_of_patients_covered_sex,
                selected = "overall",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_proportion_of_patients_covered_estimate_name",
                label = "Estimate name",
                choices = choices$summarise_proportion_of_patients_covered_estimate_name,
                selected = c("ppc", "ppc_lower","ppc_upper"),
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              position = "left"
            ),
            bslib::navset_card_tab(
              bslib::nav_panel(
                title = "Tidy",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::downloadButton(outputId = "summarise_proportion_of_patients_covered_tidy_download", label = "Download csv")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_proportion_of_patients_covered_tidy_columns",
                        label = "Columns",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex", "time", "cohort_table_name", "variable_name", "variable_level"),
                        selected = c("cdm_name", "cohort_name", "age_group", "sex", "time", "variable_name", "variable_level"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::checkboxInput(
                        inputId = "summarise_proportion_of_patients_covered_tidy_pivot_estimates",
                        label = "Pivot estimates",
                        value = TRUE
                      ),
                      position = "right"
                    ),
                    DT::DTOutput("summarise_proportion_of_patients_covered_tidy") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Table PPC",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_proportion_of_patients_covered_table_format",
                        label = "Format",
                        choices = c("docx", "png", "pdf", "html"),
                        selected = "docx",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::downloadButton(outputId = "summarise_proportion_of_patients_covered_table_download", label = "Download table")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      sortable::bucket_list(
                        header = NULL,
                        sortable::add_rank_list(
                          text = "None",
                          labels = c("time", "estimate_name"),
                          input_id = "summarise_proportion_of_patients_covered_table_none"
                        ),
                        sortable::add_rank_list(
                          text = "Header",
                          labels = c("cohort_name", "age_group", "sex"),
                          input_id = "summarise_proportion_of_patients_covered_table_header"
                        ),
                        sortable::add_rank_list(
                          text = "Group columns",
                          labels = "cdm_name",
                          input_id = "summarise_proportion_of_patients_covered_table_group_column"
                        ),
                        sortable::add_rank_list(
                          text = "Hide",
                          labels = c("variable_name", "variable_level", "cohort_table_name"),
                          input_id = "summarise_proportion_of_patients_covered_table_hide"
                        )
                      ),
                      position = "right"
                    ),
                    gt::gt_output("summarise_proportion_of_patients_covered_table") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Plot PPC",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::numericInput(
                        inputId = "summarise_proportion_of_patients_covered_plot_width",
                        label = "Width",
                        value = 15
                      ),
                      shiny::numericInput(
                        inputId = "summarise_proportion_of_patients_covered_plot_height",
                        label = "Height",
                        value = 15
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_proportion_of_patients_covered_plot_units",
                        label = "Units",
                        choices = c("px", "cm", "inch"),
                        selected = "cm",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::numericInput(
                        inputId = "summarise_proportion_of_patients_covered_plot_dpi",
                        label = "DPI",
                        value = 300
                      ),
                      shiny::downloadButton(outputId = "summarise_proportion_of_patients_covered_plot_download", label = "Download plot")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shiny::checkboxInput(
                        inputId = "summarise_proportion_of_patients_covered_plot_ribbon",
                        label = "Ribbon",
                        value = FALSE
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_proportion_of_patients_covered_plot_facet",
                        label = "Facet",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex"),
                        selected = "cohort_name",
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_proportion_of_patients_covered_plot_colour",
                        label = "Colour",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex"),
                        selected = c("age_group", "sex"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      position = "right"
                    ),
                    shiny::plotOutput("summarise_proportion_of_patients_covered_plot") |>
                      shinycssloaders::withSpinner()
                  )
                )
              )
            )
          )
        ),
        bslib::nav_panel(
          title = "Drug Restart",
          icon = shiny::icon("chart-gantt"),
          bslib::layout_sidebar(
            sidebar = bslib::sidebar(
              shinyWidgets::pickerInput(
                inputId = "summarise_drug_restart_cdm_name",
                label = "CDM name",
                choices = choices$summarise_drug_restart_cdm_name,
                selected = selected$summarise_drug_restart_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_drug_restart_cohort_name",
                label = "Cohort name",
                choices = choices$summarise_drug_restart_cohort_name,
                selected = selected$summarise_drug_restart_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_drug_restart_age_group",
                label = "Age group",
                choices = choices$summarise_drug_restart_age_group,
                selected = "overall",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_drug_restart_sex",
                label = "Sex",
                choices = choices$summarise_drug_restart_sex,
                selected = "overall",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_drug_restart_estimate_name",
                label = "Estimate name",
                choices = choices$summarise_drug_restart_estimate_name,
                selected = selected$summarise_drug_restart_estimate_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              position = "left"
            ),
            bslib::navset_card_tab(
              bslib::nav_panel(
                title = "Tidy",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::downloadButton(outputId = "summarise_drug_restart_tidy_download", label = "Download csv")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_drug_restart_tidy_columns",
                        label = "Columns",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex", "follow_up_days", "censor_date", "cohort_table_name", "incident", "restrict_to_first_discontinuation", "switch_cohort_table", "variable_name", "variable_level"),
                        selected = c("cdm_name", "cohort_name", "age_group", "sex", "follow_up_days", "variable_name", "variable_level"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::checkboxInput(
                        inputId = "summarise_drug_restart_tidy_pivot_estimates",
                        label = "Pivot estimates",
                        value = TRUE
                      ),
                      position = "right"
                    ),
                    DT::DTOutput("summarise_drug_restart_tidy") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Table Drug Restart",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_drug_restart_table_format",
                        label = "Format",
                        choices = c("docx", "png", "pdf", "html"),
                        selected = "docx",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::downloadButton(outputId = "summarise_drug_restart_table_download", label = "Download table")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      sortable::bucket_list(
                        header = NULL,
                        sortable::add_rank_list(
                          text = "None",
                          labels = c("variable_level", "age_group", "sex", "follow_up_days"),
                          input_id = "summarise_drug_restart_table_none"
                        ),
                        sortable::add_rank_list(
                          text = "Header",
                          labels = c("cdm_name", "cohort_name"),
                          input_id = "summarise_drug_restart_table_header"
                        ),
                        sortable::add_rank_list(
                          text = "Group columns",
                          labels = "variable_name",
                          input_id = "summarise_drug_restart_table_group_column"
                        ),
                        sortable::add_rank_list(
                          text = "Hide",
                          labels = c("censor_date", "cohort_table_name",
                                     "incident", "restrict_to_first_discontinuation", 
                                     "switch_cohort_table"),
                          input_id = "summarise_drug_restart_table_hide"
                        )
                      ),
                      position = "right"
                    ),
                    gt::gt_output("summarise_drug_restart_table") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Plot Drug Restart",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::numericInput(
                        inputId = "summarise_drug_restart_plot_width",
                        label = "Width",
                        value = 15
                      ),
                      shiny::numericInput(
                        inputId = "summarise_drug_restart_plot_height",
                        label = "Height",
                        value = 15
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_drug_restart_plot_units",
                        label = "Units",
                        choices = c("px", "cm", "inch"),
                        selected = "cm",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::numericInput(
                        inputId = "summarise_drug_restart_plot_dpi",
                        label = "DPI",
                        value = 300
                      ),
                      shiny::downloadButton(outputId = "summarise_drug_restart_plot_download", label = "Download plot")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_drug_restart_plot_facet",
                        label = "Facet",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex", "variable_name", "variable_level", "follow_up_days", "censor_date", "cohort_table_name", "incident", "restrict_to_first_discontinuation", "switch_cohort_table"),
                        selected = c("cdm_name", "cohort_name", "follow_up_days", "age_group", "sex"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_drug_restart_plot_colour",
                        label = "Colour",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex", "variable_name", "variable_level", "follow_up_days", "censor_date", "cohort_table_name", "incident", "restrict_to_first_discontinuation", "switch_cohort_table"),
                        selected = "variable_level",
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      position = "right"
                    ),
                    shiny::plotOutput("summarise_drug_restart_plot") |>
                      shinycssloaders::withSpinner()
                  )
                )
              )
            )
          )
        ),
        bslib::nav_panel(
          title = "Drug Utilisation",
          icon = shiny::icon("capsules"),
          bslib::layout_sidebar(
            sidebar = bslib::sidebar(
              shinyWidgets::pickerInput(
                inputId = "summarise_drug_utilisation_cdm_name",
                label = "CDM name",
                choices = choices$summarise_drug_utilisation_cdm_name,
                selected = selected$summarise_drug_utilisation_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_drug_utilisation_age_group",
                label = "Age group",
                choices = choices$summarise_drug_utilisation_age_group,
                selected = "overall",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_drug_utilisation_sex",
                label = "Sex",
                choices = choices$summarise_drug_utilisation_sex,
                selected = "overall",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_drug_utilisation_ingredient",
                label = "Ingredient",
                choices = choices$summarise_drug_utilisation_ingredient,
                selected = selected$summarise_drug_utilisation_ingredient,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_drug_utilisation_concept_set",
                label = "Concept set",
                choices = choices$summarise_drug_utilisation_concept_set,
                selected = selected$summarise_drug_utilisation_concept_set,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_drug_utilisation_variable_name",
                label = "Variable name",
                choices = choices$summarise_drug_utilisation_variable_name,
                selected = selected$summarise_drug_utilisation_variable_name,,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_drug_utilisation_estimate_name",
                label = "Estimate name",
                choices = choices$summarise_drug_utilisation_estimate_name,
                selected = selected$summarise_drug_utilisation_estimate_name[!grepl("density", selected$summarise_drug_utilisation_estimate_name)],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              position = "left"
            ),
            bslib::navset_card_tab(
              bslib::nav_panel(
                title = "Tidy",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::downloadButton(outputId = "summarise_drug_utilisation_tidy_download", label = "Download csv")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_drug_utilisation_tidy_columns",
                        label = "Columns",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex", "ingredient", "cohort_table_name", "gap_era", "index_date", "restrict_incident", "variable_name"),
                        selected = c("cdm_name", "age_group", "sex", "ingredient", "variable_name"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::checkboxInput(
                        inputId = "summarise_drug_utilisation_tidy_pivot_estimates",
                        label = "Pivot estimates",
                        value = TRUE
                      ),
                      position = "right"
                    ),
                    DT::DTOutput("summarise_drug_utilisation_tidy") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Table Drug Utilisation",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_drug_utilisation_table_format",
                        label = "Format",
                        choices = c("docx", "png", "pdf", "html"),
                        selected = "docx",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::downloadButton(outputId = "summarise_drug_utilisation_table_download", label = "Download table")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      sortable::bucket_list(
                        header = NULL,
                        sortable::add_rank_list(
                          text = "None",
                          labels = c("concept_set", "ingredient", "variable_name", "estimate_name"),
                          input_id = "summarise_drug_utilisation_table_none"
                        ),
                        sortable::add_rank_list(
                          text = "Header",
                          labels = "cdm_name",
                          input_id = "summarise_drug_utilisation_table_header"
                        ),
                        sortable::add_rank_list(
                          text = "Group columns",
                          labels = c("cohort_name", "age_group", "sex"),
                          input_id = "summarise_drug_utilisation_table_group_column"
                        ),
                        sortable::add_rank_list(
                          text = "Hide",
                          labels = c("censor_date", "cohort_table_name", "gap_era", "index_date",
                                     "restrict_incident"),
                          input_id = "summarise_drug_utilisation_table_hide"
                        )
                      ),
                      position = "right"
                    ),
                    gt::gt_output("summarise_drug_utilisation_table") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Plot Drug Utilisation",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::numericInput(
                        inputId = "summarise_drug_utilisation_plot_width",
                        label = "Width",
                        value = 15
                      ),
                      shiny::numericInput(
                        inputId = "summarise_drug_utilisation_plot_height",
                        label = "Height",
                        value = 15
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_drug_utilisation_plot_units",
                        label = "Units",
                        choices = c("px", "cm", "inch"),
                        selected = "cm",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::numericInput(
                        inputId = "summarise_drug_utilisation_plot_dpi",
                        label = "DPI",
                        value = 300
                      ),
                      shiny::downloadButton(outputId = "summarise_drug_utilisation_plot_download", label = "Download plot")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_drug_utilisation_plot_variable",
                        label = "Variable",
                        choices = c("number records", "number subjects", "cohort name", "number exposures", "time to exposure", "cumulative quantity", "initial quantity", "initial exposure duration", "number eras", "days exposed", "days prescribed", "cumulative dose milligram", "initial daily dose milligram"),
                        selected = "number exposures",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_drug_utilisation_plot_plot_type",
                        label = "Plot type",
                        choices = c("scatterplot", "barplot", "boxplot"),
                        selected = "boxplot",
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_drug_utilisation_plot_facet",
                        label = "Facet",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex", "concept_set", "ingredient", "censor_date", "cohort_table_name", "gap_era", "index_date", "restrict_incident"),
                        selected = c("age_group", "sex"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_drug_utilisation_plot_colour",
                        label = "Colour",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex", "concept_set", "ingredient", "censor_date", "cohort_table_name", "gap_era", "index_date", "restrict_incident"),
                        selected = "cohort_name",
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      position = "right"
                    ),
                    shiny::plotOutput("summarise_drug_utilisation_plot") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              # bslib::nav_panel(
              #   title = "Plot Drug Utilisation (density)",
              #   bslib::card(
              #     full_screen = TRUE,
              #     bslib::card_header(
              #       bslib::popover(
              #         shiny::icon("download"),
              #         shiny::numericInput(
              #           inputId = "summarise_drug_utilisation_density_plot_width",
              #           label   = "Width",
              #           value   = 15
              #         ),
              #         shiny::numericInput(
              #           inputId = "summarise_drug_utilisation_density_plot_height",
              #           label   = "Height",
              #           value   = 15
              #         ),
              #         shinyWidgets::pickerInput(
              #           inputId = "summarise_drug_utilisation_density_plot_units",
              #           label   = "Units",
              #           choices = c("px", "cm", "inch"),
              #           selected = "cm",
              #           multiple = FALSE,
              #           options  = list(`actions-box` = TRUE, size = 10,
              #                           `selected-text-format` = "count > 3")
              #         ),
              #         shiny::numericInput(
              #           inputId = "summarise_drug_utilisation_density_plot_dpi",
              #           label   = "DPI",
              #           value   = 300
              #         ),
              #         shiny::downloadButton(
              #           outputId = "summarise_drug_utilisation_density_plot_download",
              #           label    = "Download plot"
              #         )
              #       ),
              #       class = "text-end"
              #     ),
              #     bslib::layout_sidebar(
              #       sidebar = bslib::sidebar(
              #         shinyWidgets::pickerInput(
              #           inputId = "summarise_drug_utilisation_density_plot_variable",
              #           label   = "Variable",
              #           choices = c("time to exposure",
              #                       "number exposures",
              #                       "cumulative quantity",
              #                       "initial quantity",
              #                       "initial exposure duration", 
              #                       "number eras",
              #                       "days exposed", "days prescribed",
              #                       "cumulative dose milligram",
              #                       "initial daily dose milligram"),
              #           selected = "initial daily dose milligram",
              #           multiple = FALSE,
              #           options  = list(`actions-box` = TRUE, size = 10,
              #                           `selected-text-format` = "count > 3")
              #         ),
              #         shinyWidgets::pickerInput(
              #           inputId = "summarise_drug_utilisation_density_plot_plot_type",
              #           label   = "Plot type",
              #           choices = c("densityplot"),
              #           selected = "barplot",
              #           options  = list(`actions-box` = TRUE, size = 10,
              #                           `selected-text-format` = "count > 3")
              #         ),
              #         shinyWidgets::pickerInput(
              #           inputId = "summarise_drug_utilisation_density_plot_facet",
              #           label   = "Facet",
              #           choices = c("cdm_name", "cohort_name", "age_group", "sex",
              #                       "concept_set", "ingredient", "censor_date",
              #                       "cohort_table_name", "gap_era", "index_date",
              #                       "restrict_incident"),
              #           selected = c("age_group", "sex"),
              #           multiple = TRUE,
              #           options  = list(`actions-box` = TRUE, size = 10,
              #                           `selected-text-format` = "count > 3")
              #         ),
              #         shinyWidgets::pickerInput(
              #           inputId = "summarise_drug_utilisation_density_plot_colour",
              #           label   = "Colour",
              #           choices = c("cdm_name", "cohort_name", "age_group", "sex",
              #                       "concept_set", "ingredient", "censor_date",
              #                       "cohort_table_name", "gap_era", "index_date",
              #                       "restrict_incident"),
              #           selected = "cohort_name",
              #           multiple = TRUE,
              #           options  = list(`actions-box` = TRUE, size = 10,
              #                           `selected-text-format` = "count > 3")
              #         ),
              #         position = "right"
              #       ),
              #       shiny::plotOutput("summarise_drug_utilisation_density_plot") |>
              #         shinycssloaders::withSpinner()
              #     )
              #   )
              # )
            )
          )
        ),
        bslib::nav_panel(
          title = "Indication",
          icon = shiny::icon("disease"),
          bslib::layout_sidebar(
            sidebar = bslib::sidebar(
              shinyWidgets::pickerInput(
                inputId = "summarise_indication_cdm_name",
                label = "CDM name",
                choices = choices$summarise_indication_cdm_name,
                selected = selected$summarise_indication_cdm_name[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_indication_age_group",
                label = "Age group",
                choices = choices$summarise_indication_age_group,
                selected = "overall",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_indication_sex",
                label = "Sex",
                choices = choices$summarise_indication_sex,
                selected = "overall",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_indication_variable_name",
                label = "Variable name",
                choices = choices$summarise_indication_variable_name,
                selected = selected$summarise_indication_variable_name[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_indication_estimate_name",
                label = "Estimate name",
                choices = choices$summarise_indication_estimate_name,
                selected = "percentage",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_indication_mutually_exclusive",
                label = "Mutually exclusive",
                choices = choices$summarise_indication_mutually_exclusive,
                selected = selected$summarise_indication_mutually_exclusive,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              position = "left"
            ),
            bslib::navset_card_tab(
              bslib::nav_panel(
                title = "Tidy",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::downloadButton(outputId = "summarise_indication_tidy_download", label = "Download csv")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_indication_tidy_columns",
                        label = "Columns",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex", "window_name", "censor_date", "cohort_table_name", "index_date", "indication_cohort_name", "mutually_exclusive", "unknown_indication_table", "variable_name", "variable_level"),
                        selected = c("cdm_name", "cohort_name", "age_group", "sex", "window_name", "variable_name", "variable_level"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::checkboxInput(
                        inputId = "summarise_indication_tidy_pivot_estimates",
                        label = "Pivot estimates",
                        value = TRUE
                      ),
                      position = "right"
                    ),
                    DT::DTOutput("summarise_indication_tidy") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Table Indication",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_indication_table_format",
                        label = "Format",
                        choices = c("docx", "png", "pdf", "html"),
                        selected = "docx",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::downloadButton(outputId = "summarise_indication_table_download", label = "Download table")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      sortable::bucket_list(
                        header = NULL,
                        sortable::add_rank_list(
                          text = "None",
                          labels = c("variable_level", "estimate_name"),
                          input_id = "summarise_indication_table_none"
                        ),
                        sortable::add_rank_list(
                          text = "Header",
                          labels = c("cdm_name", "cohort_name", "age_group", "sex"),
                          input_id = "summarise_indication_table_header"
                        ),
                        sortable::add_rank_list(
                          text = "Group columns",
                          labels = "variable_name",
                          input_id = "summarise_indication_table_group_column"
                        ),
                        sortable::add_rank_list(
                          text = "Hide",
                          labels = c("window_name", "censor_date", "cohort_table_name", "index_date", "indication_cohort_name", "mutually_exclusive", "unknown_indication_table"),
                          input_id = "summarise_indication_table_hide"
                        )
                      ),
                      position = "right"
                    ),
                    gt::gt_output("summarise_indication_table") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Plot Indication",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::numericInput(
                        inputId = "summarise_indication_plot_width",
                        label = "Width",
                        value = 15
                      ),
                      shiny::numericInput(
                        inputId = "summarise_indication_plot_height",
                        label = "Height",
                        value = 15
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_indication_plot_units",
                        label = "Units",
                        choices = c("px", "cm", "inch"),
                        selected = "cm",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::numericInput(
                        inputId = "summarise_indication_plot_dpi",
                        label = "DPI",
                        value = 300
                      ),
                      shiny::downloadButton(outputId = "summarise_indication_plot_download", label = "Download plot")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_indication_plot_facet",
                        label = "Facet",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex", "window_name", "censor_date", "cohort_table_name", "index_date", "indication_cohort_name", "mutually_exclusive", "unknown_indication_table"),
                        selected = c("cdm_name", "cohort_name", "age_group", "sex", "window_name"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      position = "right"
                    ),
                    shiny::plotOutput("summarise_indication_plot") |>
                      shinycssloaders::withSpinner()
                  )
                )
              )
            )
          )
        ),
        bslib::nav_panel(
          title = "Treatments",
          icon = shiny::icon("disease"),
          bslib::layout_sidebar(
            sidebar = bslib::sidebar(
              shinyWidgets::pickerInput(
                inputId = "summarise_treatment_cdm_name",
                label = "CDM name",
                choices = choices$summarise_treatment_cdm_name,
                selected = selected$summarise_treatment_cdm_name[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_treatment_age_group",
                label = "Age group",
                choices = choices$summarise_treatment_age_group,
                selected = "overall",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_treatment_sex",
                label = "Sex",
                choices = choices$summarise_treatment_sex,
                selected = "overall",
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_treatment_variable_name",
                label = "Variable name",
                choices = choices$summarise_treatment_variable_name,
                selected = selected$summarise_treatment_variable_name[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_treatment_estimate_name",
                label = "Estimate name",
                choices = choices$summarise_treatment_estimate_name,
                selected = selected$summarise_treatment_estimate_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              shinyWidgets::pickerInput(
                inputId = "summarise_treatment_index_date",
                label = "Index date",
                choices = choices$summarise_treatment_index_date,
                selected = selected$summarise_treatment_index_date[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
              ),
              position = "left"
            ),
            bslib::navset_card_tab(
              bslib::nav_panel(
                title = "Tidy",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::downloadButton(outputId = "summarise_treatment_tidy_download", label = "Download csv")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_treatment_tidy_columns",
                        label = "Columns",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex", "window_name", "censor_date", "cohort_table_name", "index_date", "mutually_exclusive", "treatment_cohort_name", "variable_name", "variable_level"),
                        selected = c("cdm_name", "cohort_name", "age_group", "sex", "window_name", "variable_name", "variable_level"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::checkboxInput(
                        inputId = "summarise_treatment_tidy_pivot_estimates",
                        label = "Pivot estimates",
                        value = TRUE
                      ),
                      position = "right"
                    ),
                    DT::DTOutput("summarise_treatment_tidy") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Table Treatments",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_treatment_table_format",
                        label = "Format",
                        choices = c("docx", "png", "pdf", "html"),
                        selected = "docx",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::downloadButton(outputId = "summarise_treatment_table_download", label = "Download table")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      sortable::bucket_list(
                        header = NULL,
                        sortable::add_rank_list(
                          text = "None",
                          labels = c("variable_level", "estimate_name"),
                          input_id = "summarise_treatment_table_none"
                        ),
                        sortable::add_rank_list(
                          text = "Header",
                          labels = c("cdm_name", "cohort_name", "age_group", "sex"),
                          input_id = "summarise_treatment_table_header"
                        ),
                        sortable::add_rank_list(
                          text = "Group columns",
                          labels = "variable_name",
                          input_id = "summarise_treatment_table_group_column"
                        ),
                        sortable::add_rank_list(
                          text = "Hide",
                          labels = c("window_name", "censor_date", "cohort_table_name", "index_date", "mutually_exclusive", "treatment_cohort_name"),
                          input_id = "summarise_treatment_table_hide"
                        )
                      ),
                      position = "right"
                    ),
                    gt::gt_output("summarise_treatment_table") |>
                      shinycssloaders::withSpinner()
                  )
                )
              ),
              bslib::nav_panel(
                title = "Plot Treatment",
                bslib::card(
                  full_screen = TRUE,
                  bslib::card_header(
                    bslib::popover(
                      shiny::icon("download"),
                      shiny::numericInput(
                        inputId = "summarise_treatment_plot_width",
                        label = "Width",
                        value = 15
                      ),
                      shiny::numericInput(
                        inputId = "summarise_treatment_plot_height",
                        label = "Height",
                        value = 15
                      ),
                      shinyWidgets::pickerInput(
                        inputId = "summarise_treatment_plot_units",
                        label = "Units",
                        choices = c("px", "cm", "inch"),
                        selected = "cm",
                        multiple = FALSE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      shiny::numericInput(
                        inputId = "summarise_treatment_plot_dpi",
                        label = "DPI",
                        value = 300
                      ),
                      shiny::downloadButton(outputId = "summarise_treatment_plot_download", label = "Download plot")
                    ),
                    class = "text-end"
                  ),
                  bslib::layout_sidebar(
                    sidebar = bslib::sidebar(
                      shinyWidgets::pickerInput(
                        inputId = "summarise_treatment_plot_facet",
                        label = "Facet",
                        choices = c("cdm_name", "cohort_name", "age_group", "sex", "window_name", "censor_date", "cohort_table_name", "index_date", "mutually_exclusive", "treatment_cohort_name"),
                        selected = c("cdm_name", "cohort_name", "age_group", "sex", "window_name"),
                        multiple = TRUE,
                        options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3")
                      ),
                      position = "right"
                    ),
                    shiny::plotOutput("summarise_treatment_plot") |>
                      shinycssloaders::withSpinner()
                  )
                )
              )
            )
          )
        ))
    )),
  bslib::nav_spacer(),
  bslib::nav_item(
    bslib::popover(
      shiny::icon("download"),
      shiny::downloadButton(
        outputId = "download_raw",
        label = "Download raw data",
        icon = shiny::icon("download")
      )
    )
  ),
  bslib::nav_item(
    bslib::popover(
      shiny::icon("circle-info"),
      shiny::tags$img(
        src = "hds_logo.svg",
        class = "logo-img",
        alt = "Logo",
        height = "auto",
        width = "30%",
        style = "float:right"
      ),
      "This shiny app was generated with ",
      shiny::a(
        "OmopViewer",
        href = "https://github.com/OHDSI/OmopViewer",
        target = "_blank"
      ),
      shiny::strong("v0.3.0")
    )
  ),
  bslib::nav_item(bslib::input_dark_mode(id = "dark_mode", mode = "light"))
)
